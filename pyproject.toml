[project]
name = "face-detection"
version = "1.0.0"
description = "AURA Vision — AI-powered Age, Gender & Emotion Detection System"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.22.0",
]

[tool.vercel]
entrypoint = "main:app"
